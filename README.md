# Running the project

## Recommended (Development mode)

npm install
npm start

Then open:
http://localhost:4200

## Note about build

The Angular CLI (version 21) requires Node.js v20.19 or v22.12 minimum.
npm run build will fail on Node.js versions below v20.

Backend must be running:
./mvnw spring-boot:run
